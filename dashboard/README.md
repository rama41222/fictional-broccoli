# Cyling and Weather Dashboard
## Setup

In the project directory, you can run:

```bash

yarn start # Start the project in a random port

yarn test # Runs all the unit tests

yarn build # Builds the react app

yarn eject # Remove a build dependancy
```

